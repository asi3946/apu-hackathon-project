# 高機能Vimライク・共有メモ帳

## 目的
テーマ「越境と交流」に対し、Vimの操作性と共同編集機能を備えたWebメモ帳を開発します。マイクロソフトのメモ帳を超える使い勝手を目指し、以下のコア機能でユーザー間のシームレスな交流を実現します。

- NotionライクなブロックベースのVim操作
- AIを用いた自動タイトル・タグ生成による情報整理
- 複数人でのリアルタイム共同編集

## 使用技術と選定理由

### フロントエンド
- Next.js: アプリケーション全体のUI構築
- Zustand: ブロックごとのデータ構造と、Vimのモード状態（ノーマル・インサートなど）のグローバルな状態管理
- Lucide: 軽量で統一感のあるUIアイコン
- Zod: AIから返却されるJSONデータの型安全なバリデーション

### バックエンド (Supabase)
- Database / Auth: メモデータの保存と、安全な共有のためのユーザー認証基盤
- Realtime: 共同編集時のブロック状態の即時同期や、現在誰がメモを開いているかの表示（プレゼンス機能）
- Edge Functions + Webhooks: メモ保存時にバックグラウンドでAIモデルを呼び出し、自動でタグとタイトルを付与（Vercelの実行時間制限を回避するアーキテクチャ）

### インフラ・デプロイ
- Vercel: フロントエンドのホスティング環境

## 開発の要点（MVPに向けた方針）
- エディタは単一のテキストエリアではなく、行ごとのブロック配列としてZustandで管理します。
- Vimの操作はブロック間の移動（j, k）や、特定ブロックの入力モード切替（i, Esc）として実装し、開発工数を抑えます。
- 共同編集においては、誰かが編集中のブロックのみをロックする仕組みにすることで、複雑な競合を避けつつリアルタイムな共有体験を作ります。

## ローカル環境の構築手順
(環境構築担当者がここにコマンドなどを追記します。例: npm install, npm run devなど)

## 開発の進め方とルール

### 1. フォーマットとLint (Biome)
プロジェクトのLinterおよびFormatterとしてBiomeを採用しています。
基本的には各自のエディタの保存時自動フォーマット機能を使用してください。
設定がうまくいかない場合や、コミット前にプロジェクト全体を一括で整えたい場合は、ターミナルで以下のコマンドを実行してください。

npx biome check --write .

### 2. Gitブランチ戦略
- mainブランチ: 本番環境用です。直接のプッシュは禁止とします。
- developブランチ: 開発のベースラインです。各機能はこのブランチから派生させます。
- 作業ブランチ: developから派生させ、ブランチ名は「issue/課題番号」としてください（例: issue/11）。

### 3. GitHub CLI (gh) の利用
日常的なGit操作（PRの作成やIssueの確認など）は、ブラウザを開かずに済むGitHub CLIの使用を標準とします。
PRを作成する際は、作業ブランチ（issue/番号）からdevelopブランチに向けて作成してください。

よく使うコマンド例:
- gh issue list : 割り当てられているIssueの一覧を確認
- gh pr create : 作業中のブランチからPRを作成
- gh pr view --web : ターミナルで扱っているPRのブラウザ画面を開く

### 4. コミットメッセージの規則
変更内容がひと目でわかるよう、コミットメッセージの先頭に以下のプレフィックスをつけてください。
issue番号を最後に記述してください。
- feat: 新機能の追加 #13
- fix: バグの修正 #79
- ui: デザインやUIコンポーネントのみの修正・変更 #24
- refactor: 機能追加やバグ修正を伴わないコードの整理 #35
- docs: READMEなどドキュメントの修正 #59
- chore: ビルドプロセスや設定ファイルなどの細かな変更 #28
